/** Double submit module for some cases */

import * as S from '@/store/submit'

export const mt = S.mt
export const state = S.state
export const strict = S.strict
export type Submit = S.Submit
export const getters = S.getters
export const mutations = S.mutations
export const actions = S.actions
